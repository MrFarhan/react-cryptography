module.exports = {
    // Other configurations...
    resolve: {
        fallback: {
            "assert": require.resolve("assert/")
        }
    },
    resolve: {
        fallback: {
            "constants": false
        }
    }


};
